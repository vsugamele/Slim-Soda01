# ============================================================
# SLIM-SODA01 REPO — APENAS O QUE VAI PRO DEPLOY
# Estratégia: WHITELIST (tudo é ignorado, exceto o deployable)
# ============================================================

# Ignora TUDO por padrão (mantém o repo-mãe intacto, só o Slim-Soda01-deployable vai)
*

# Libera APENAS o que vai pro deploy em produção
!/.gitignore
!/README.md
!/index.html
!/vercel.json
!/js/
!/js/**
!/images/
!/images/**
!/slimtide-vsl/
!/slimtide-vsl/**
!/buy-page/
!/buy-page/**
!/chat-x1/
!/chat-x1/**
!/api/
!/api/**

# Mas ignora lixo mesmo dentro dos diretórios permitidos
index.html.bak
images/*.tmp
images/*.bak
slimtide-vsl/*.log
buy-page/*.log

# Mantém o resto do projeto-mãe INTACTO (não commita nada além do Slim-Soda01-deployable)

# DR Reference HUB v23+ (briefings, methodology, tracking)
!/references_v5/
!/references_v5/**
